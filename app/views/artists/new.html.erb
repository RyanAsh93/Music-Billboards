<h1>Artists#new</h1>
<p>Add New Artist</p>

<%= form_with(model: [@billboard, @artist], local: true) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>
<%= link_to "Back", billboard_path(@billboard) %>
